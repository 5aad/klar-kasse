diff --git a/utils/build/createManifestForBuildAsync.js b/utils/build/createManifestForBuildAsync.js
index 4c7d343..7f3be0d 100644
--- a/utils/build/createManifestForBuildAsync.js
+++ b/utils/build/createManifestForBuildAsync.js
@@ -17,8 +17,11 @@ async function createManifestForBuildAsync(platform, projectRoot, destinationDir
         (0, paths_1.resolveRelativeEntryPoint)(projectRoot, { platform }) ||
         'index.js';
     // Android uses absolute paths for the entry file, so we need to convert that to a relative path.
     if (path_1.default.isAbsolute(entryFile)) {
-        entryFile = (0, paths_1.convertEntryPointToRelative)(projectRoot, entryFile);
+        const relativeEntryFile = path_1.default.relative(projectRoot, entryFile);
+        entryFile = relativeEntryFile && !relativeEntryFile.startsWith('..') && !path_1.default.isAbsolute(relativeEntryFile)
+            ? relativeEntryFile
+            : (0, paths_1.convertEntryPointToRelative)(projectRoot, entryFile);
     }
     process.chdir(projectRoot);
     const options = {
diff --git a/utils/src/createManifestForBuildAsync.ts b/utils/src/createManifestForBuildAsync.ts
index 8d8aa42..629ff4c 100644
--- a/utils/src/createManifestForBuildAsync.ts
+++ b/utils/src/createManifestForBuildAsync.ts
@@ -27,7 +27,11 @@ export async function createManifestForBuildAsync(
 
   // Android uses absolute paths for the entry file, so we need to convert that to a relative path.
   if (path.isAbsolute(entryFile)) {
-    entryFile = convertEntryPointToRelative(projectRoot, entryFile);
+    const relativeEntryFile = path.relative(projectRoot, entryFile);
+    entryFile =
+      relativeEntryFile && !relativeEntryFile.startsWith('..') && !path.isAbsolute(relativeEntryFile)
+        ? relativeEntryFile
+        : convertEntryPointToRelative(projectRoot, entryFile);
   }
 
   process.chdir(projectRoot);
